import Login from "./pages/Login";

/**
 * The main-single page of our application.
 * Only contains the Login component because users need to start login in
 * before using the application.
 *
 * @returns {JSX.Element} Login page component.
 * @constructor
 */
function App() {
    return (
        <Login/>
    );
}
export default App;
